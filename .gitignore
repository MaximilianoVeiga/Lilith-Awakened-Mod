bin/
obj/
.vs/
*.user
*.suo

# Local configuration and credentials
BepInEx/config/
BepInEx/data/LilithTextInjector/memory/
*.key
*.secret
.env

# Release binaries belong in GitHub Releases, not repository history
LilithAI-Mod-Setup.exe
packages/
*.zip
artifacts/
references/

# Nested assets git repo (voice-pack + Lilith models)
Lilith-Awakened-Assets/

# Local BepInEx refs used only for building the injector (populated by pack script)
src/bepinex-be780/
# Upstream Unity player zip (extract DLLs only; do not commit)
release-assets/unity-libs/*.zip

